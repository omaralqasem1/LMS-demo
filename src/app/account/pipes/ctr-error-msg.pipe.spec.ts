import { CtrErrorMsgPipe } from './ctr-error-msg.pipe';

describe('CtrErrorMsgPipe', () => {
  it('create an instance', () => {
    const pipe = new CtrErrorMsgPipe();
    expect(pipe).toBeTruthy();
  });
});
